apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache-service
  namespace: pokemon-drafter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cache-service
  template:
    metadata:
      labels:
        app: cache-service
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        command: ["redis-server", "--appendonly", "yes"]
        volumeMounts:
        - name: redis-storage
          mountPath: /data
      volumes:
      - name: redis-storage
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: cache-service
  namespace: pokemon-drafter
spec:
  selector:
    app: cache-service
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
